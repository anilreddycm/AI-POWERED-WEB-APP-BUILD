{
  "name": "ai-powered-web-app-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root for AI Powered Web App Builder – contains client and server sub‑projects.",
  "scripts": {
    "install": "npm install --prefix client && npm install --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "dev": "npm run dev --prefix client & npm run dev --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
